# Shell scripts and Docker/Nginx configs run on Linux — force LF so they don't
# break with Windows CRLF line endings.
*.sh        text eol=lf
*.conf      text eol=lf
*.service   text eol=lf
deploy/**   text eol=lf
